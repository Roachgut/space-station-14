using Content.Shared._ClawCommand.InteractionVerbs;

namespace Content.Client._ClawCommand.InteractionVerbs;

// Exists because the shared system is abstract and requires a concrete implementation.
public sealed class InteractionVerbsSystem : SharedInteractionVerbsSystem
{
}
